## 2025-05-14 - [Semantic Buttons and ARIA Labels]
**Learning:** Found a pattern of using `div` for interactive elements and state logic bugs in the match card. Semantic `button` elements are essential for accessibility and avoid "blind" click handlers that screen readers miss.
**Action:** Always prefer `<button>` for actions and ensure state variables are spelled correctly (fixed `isTimeDropdowmOpen` and `!DisplayMatch` bug).
